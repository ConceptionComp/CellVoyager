You will be provided a summary of a research paper as well as a set of computational analyses that were previously attempted for the given single-cell transcriptomic dataset, provided as a Monocle3 cell_data_set.
Your role is to provide a new computational analysis plan that is completely distinct from the analyses in the paper AND from those that were previously attempted.
Propose analyses that you think are missing from the paper. Ensure these are standard single-cell analyses.
Specifically, you will return a hypothesis, a series of analysis steps towards testing that hypothesis, and finally the R (monocle3) code for executing the first analysis step.

Return exactly one JSON object and nothing else.
Your final answer must start with `{{` and end with `}}`.
Do not include markdown fences, prose, bullet points, or visible reasoning.
Do not add any keys beyond `hypothesis`, `analysis_plan`, `first_step_code`, `code_description`, and `summary`.

{CODING_GUIDELINES}

For the analysis plan, think of the analysis plan as a scientific workflow:
    1. Start with exploratory data analysis that is broad and tests many things. Ground every subset and `group_cells_by`/`model_formula_str` in the REAL column names and category values — these are listed in the cds summary below (each colData column with example values), so use them verbatim and never invent a column or level (e.g. a cell line may be in `cellline_detail`, not `Cell.Line`; cell-type/cluster labels are in `assigned_cell_type`/`clusters(cds)`). The first step's code should print `colnames(colData(cds))` and `table(...)`/`unique(...)` for the columns your hypothesis relies on to confirm them. Inventing a column/level silently yields an empty subset and a figureless run.
    2. Then, focus on the more promising results from the exploratory phase by creating more focused analyses
    3. Include statistical validation of your results where appropiate
Do not number the analysis plan.
Each step in the analysis plan should be distinct from one another and could involve conducting a statistical analysis, printing information about the cell_data_set (cds), running a monocle3 workflow, etc. (the data is already loaded as `cds`).
Every result-yielding step should include at least one printed visualization of its result (e.g. an embedding/expression plot via the sourced `plot_genes_flexible`/`feature_plot_flexible` helpers, a violin/box plot by group, or a faceted/patchwork grid); prefer a plot over a bare text table.
Use at most {max_iterations} steps. Do not exceed this limit unless absolutely necessary for the analysis.

You are given the following summary of the Monocle3 cell_data_set (cds):
{adata_summary}

Here are the previous analyses attempted:
{past_analyses}

Here is a summary of the research paper:
{paper_txt}

Here is a summary of relevant biological information for finding a novel idea:
{deepresearch_background}
