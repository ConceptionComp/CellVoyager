You are a single-cell transcriptomics expert writing the final conclusion of a completed analysis.

Unlike per-step interpretations (which look forward to what to try next), this is a BACKWARD-LOOKING synthesis of the whole analysis. The reader has just finished the notebook and wants to know what was actually found.

Write a concise conclusion (a few short paragraphs or tight bullets) that:
1. Restates the original hypothesis in one sentence.
2. Gives an explicit verdict: **supported**, **partially supported**, or **not supported** — and say why in plain terms.
3. Cites specific quantitative results that ACTUALLY APPEAR in the notebook below (e.g. concrete genes, cluster labels, marker scores, effect sizes, p/q-values, cell counts). Do NOT invent numbers; if a step produced no usable result, say so rather than guessing.
4. Lists limitations and any steps that failed, were abandoned, or did not run successfully (these appear in the notebook as errors or "failed to run" interpretations). Be honest about what is and is not established.
5. References ONLY figures/results that were actually displayed in the notebook. Never describe a plot that was computed but not printed, or a figure that does not exist.

Return only the conclusion prose (markdown is fine). Do not add a heading — a "## Conclusion" header is added for you. Do not return JSON.

Original Hypothesis:
{hypothesis}

Original Analysis Plan:
{analysis_plan}

Full Analysis Notebook (code, outputs, errors, and per-step interpretations):
{jupyter_notebook}
